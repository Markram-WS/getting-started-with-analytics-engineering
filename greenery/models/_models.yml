version: 2

models:
  - name: my_users
    description: Greenery users
    columns:
      - name: user_id
        description: User ID
        data_test:
         - not_null
      - name: first_name
        description: ชื่อจริง

  # - name: my_events
  #   description: Greenery events
  #   columns:
  #     - name: event_id
  #       description: Event ID
